<%= form_with(model: lost, local: true) do |form| %>
  <% if lost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lost.errors.count, "error") %> prohibited this lost from being saved:</h2>

      <ul>
      <% lost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    
  <% end %>
   <div id="field">
    <label for="inp" class="inp">
    <%= form.text_field :title, id: :lost_title, :id => "inp", :placeholder => "분실물을 입력하세요" %>
   <span class="border"></span>
   </label>
  </div><br>
  
  <div id="field">
  <label for="inp" class="inp">
        
    <%= form.text_area :content, id: :lost_content, :id => "inp", :placeholder => "분실(습득) 장소를 입력해주세요", style: "overflow:hidden; height:170px;"%>

   </label>
  </div><br>
  
  <div id="field">
  <label for="inp" class="inp">
        
    <%= form.text_area :password, id: :lost_password, :id => "inp", :placeholder => "수정 / 삭제 시 비밀번호 설정"%>

   </label>
  </div><br>

   
  
  <div class="field">
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <div style="text-align: center; margin-top: 10px;">
    <%= form.submit "작성", class:"btn btn-secondary" %>
    </div>
  </div>
<% end %>