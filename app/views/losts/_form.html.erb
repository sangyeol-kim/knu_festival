<%= form_with(model: lost, local: true) do |form| %>
  <% if lost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lost.errors.count, "error") %> prohibited this lost from being saved:</h2>

      <ul>
      <% lost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group" style="margin-top: 10px">
    <div id="title">
    <%= form.text_field :title, id: :hunting_title, class:"form-control", placeholder:"제목을입력하세요." %>
    </div>
  </div>

  <div class="field" >
    <div id="content">
    <%= form.text_area :content, id: :hunting_content, class:"form-control", placeholder:"내용을입력하세요." %>
    </div>
  </div>
  
  <div class="field">
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <div style="text-align: right; margin-top: 10px;">
    <%= form.submit "작성", class:"btn btn-secondary", style:"margin-right: 15px;"%>
    </div>
  </div>
<% end %>