<%= form_with(model: all_notice, local: true) do |form| %>
  <% if all_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(all_notice.errors.count, "error") %> prohibited this all_notice from being saved:</h2>

      <ul>
      <% all_notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <div class="col-3"><%= form.label :"제목" %></div>
    <div class="col-9"><%= form.text_field :title, id: :all_notice_title, class: "form-control" %></div>
  </div>

  <div class="field row">
    <div class="col-3"><%= form.label :"내용" %></div>
    <%= form.text_area :content, id: :all_notice_content, class:"form-control" %>
  </div>

  <div style="margin-top: 30px;" align="center" class="actions">
    <%= form.button "글쓰기", class: "btn btn-primary" %>
    <%= link_to '돌아가기', all_notices_path %>
  </div>
<% end %>
